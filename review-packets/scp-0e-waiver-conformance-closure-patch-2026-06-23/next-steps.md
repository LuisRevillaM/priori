# Next Steps

## If Accepted

Begin SCP-1 immediately:

```text
SemanticExpression
-> compile to existing TacticalQueryDocument
-> existing binder
-> existing deterministic runtime
```

Use the two pilots already named by the roadmap:

```text
High-Bypass Completed Pass
Ball-Side Block Shift
```

Do not add new football capabilities or a new executor during the first SCP-1 milestone.

## If Rejected

Keep the correction limited to exactness and waiver/conformance gates. Do not reopen the broader architecture unless the reviewer identifies a concrete failure outside this closure-patch surface.


# Next Steps

1. Send this packet to the external reviewer with `external-review-prompt.md`.
2. If decision is `APPROVE`, unblock M1.2 planning.
3. If decision is `APPROVE_WITH_REQUIRED_CHANGES_BEFORE_M1_2`, implement only the required changes and package a focused delta packet.
4. If decision is `REJECT`, keep M1.2 blocked and update `delivery/m1.1/STRUCTURAL_CORRECTIVE_SPEC.md`, `delivery/m1.1/status.yaml`, and `delivery/ledger.jsonl`.

# Next Steps

## If Approved

1. Implement Beta 1C: expose `HERMES_NOVEL_COMPOSITION` in Workbench using the N1I/N1D.1 proof path.
2. Preserve exact provenance and source labels: `HERMES_NOVEL_COMPOSITION` must never be shown for presets, recipe selection, deterministic repair, or model-unavailable states.
3. Keep host confirmation mandatory before execution.
4. Show cache status honestly: first run should be cache miss; repeated run may be cache hit.
5. Render evidence, traces, entry-mode semantics, and replay without inferring missing geometry.
6. Produce a deployed Beta 1C smoke packet after implementation.

## If Approved With Required Changes

Make only the requested focused proof or presentation corrections, then regenerate a narrow delta packet. Avoid reopening runtime architecture unless the reviewer identifies a concrete proof contradiction.

## If Rejected

Keep Beta 1C blocked and preserve this packet as evidence. The next slice should address the named blocker without broadening scope into tactical-library expansion or UI redesign.

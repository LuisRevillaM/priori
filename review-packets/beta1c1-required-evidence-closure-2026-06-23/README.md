# Beta 1C.1 Required Evidence Closure Review Packet

## What Is This Packet?

This is an external inspection packet for the Beta 1C.1 corrective slice. It is for reviewers who do not have repository access.

Packet type: `inspection_packet_only`.

## Review Scope

Review whether Beta 1C.1 closes the external-review blocker:

```text
The attested Hermes hero execution must not be accepted while required evidence is missing.
```

The focused defect was:

```text
execution.status = incomplete
requested_evidence_failure_count = 14
possession_start_frame_id = null on every result
```

## What Changed

- Public execution responses now expose `execution_status`, `execution_complete`, `requested_evidence_failure_count`, and `requested_evidence_failures`.
- Execution records carry the same completeness fields.
- Execution cache identity was bumped to prevent stale incomplete handles from being reused.
- N1D pins required-evidence completeness as part of its deterministic identity.
- N1D.1 refuses `VERIFIED` unless both the N1D manifest and Hermes-origin bundle prove complete required evidence.
- The committed Hermes-origin bundle host pipeline was refreshed under the corrected runtime.
- Workbench renders incomplete execution visibly if it ever occurs.
- The novel-composition copy now says: `Verified Hermes-authored composition`, loaded from a previously attested Hermes session.
- Render readiness pin was updated to the regenerated knowledge-pack file hash.

## What Is Real

- The deployed service is live at `https://priori-integrated-alpha.onrender.com`.
- Corrective code commit: `cf5b058eb7e02ce4f7b22e9c56f85e64341d117b`.
- Deployed follow-up env-pin commit: `630b2b81a1fbf679dbd413d2d8e466a627645050`.
- Live smoke shows `execution_status = pass`, `execution_complete = true`, `requested_evidence_failure_count = 0`, and 14 returned results.
- The first live result contains `possession_start_frame_id = 11365`.
- Repeat live smoke shows the corrected cache key hits after the first execution.

## What Is Generated Or Local

- `artifacts/` reports are generated by verification commands.
- The packet includes copied source files and diffs, not a full repository.
- The packet cannot independently rerun tests without the full repo, Python environment, node modules, and canonical data.

## Review Map

- Start with `docs/BETA_1C_1_REQUIRED_EVIDENCE_REPORT.md`.
- Inspect `route-smokes/live-hero-smoke.json` for deployed proof.
- Inspect `artifacts/n1d-canonical-freeze-manifest.json` for pinned required-evidence identity.
- Inspect `artifacts/n1d1-attestation.json` for `VERIFIED` attestation.
- Inspect `diffs/cf5b058-focused.diff` for implementation changes.
- Inspect `validation-output.md` for commands run and results.

## Explicitly Not Proven

- This does not prove arbitrary new natural-language requests can be attested live.
- This does not add new football primitives or tactical families.
- This does not change the frozen hero question, semantics, Hermes prompt, or catalog shape.
- This does not prove video integration.

## Reviewer Question

Can Beta 1C now be accepted for the narrow claim:

```text
The Workbench can execute and replay one verified Hermes-authored experimental composition,
with complete required evidence and honest provenance.
```

# Known Gaps

## Fresh Draft Drift

Class: `unknown`

A fresh live Hermes call after `fd09735` drafted `possession_corridor_destination_entry_v1`, but the bound hash and structural fingerprint did not match the committed N1I attestation. The product therefore now exposes the committed N1I-attested plan for the exact hero question rather than labeling fresh drift as verified.

Default boundary: only committed verified attestations can produce `HERMES_NOVEL_COMPOSITION`.

Next action: design scope-independent or structure-level attestation only if broader reusable novel composition is needed.

## Cache Miss Not Reproduced In Live Smoke

Class: `not_in_scope`

The live product smoke showed cache `HIT`, not first-run `MISS`, because the protected N1I runner already executed and cached the same deterministic plan in the deployed persistent runtime.

Default boundary: do not claim first-run miss for this deployed state.

Next action: use a clean cache namespace or a new attested plan if a cache-miss proof is required.

## Scope Generalization Deferred

Class: `not_in_scope`

The attested plan is locked to `J03WOY / firstHalf / home`. User-selected scope changes are not supported for this Beta 1C proof.

Default boundary: the UI should show and enforce the locked scope.

Next action: separate scope-independent tactical structure from host-owned invocation scope in a later milestone.

## Packet Is Inspection-Only

Class: `requires_full_repo`

The packet includes source excerpts, artifacts, diffs, and outputs. It does not include the full repo, Python environment, Node environment, canonical data, or Render credentials.

Default boundary: reviewer can inspect evidence but cannot rerun all commands from the packet alone.

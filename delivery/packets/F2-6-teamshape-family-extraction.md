# Work Packet F2-6 — Team-Shape & Defending Family Extraction

Issued: 2026-07-03 by the project director. Fifth family relocation;
accepted F2-2..5 template applies verbatim (zero-drift pure relocation,
registry-only executor wiring, fences, stage commits, clean shared tree,
full-suite table, eight pinned gates).

Branch `packet/f2-6` off the frontier (tip `d3e8fa5` or later).

## Scope

Move the team-shape/defending node functions from `executor.py` to
`src/tqe/runtime/capabilities/teamshape_family.py`:

- team-press / pressure-on-carrier node function(s)
- cover-shadow node function(s)
- compactness / team-extent node function(s)
- lateral-shift / signed-lateral-shift / block-shift signal node function(s)
- local-number node function(s) if still inline
- family-only helpers move; shared helpers stay and get listed

ALSO deliver (census, no action): the complete list of capability node
functions still inline in executor.py after this move, grouped by proposed
final family — this becomes the F2-7 sweep packet's scope.

## Deliverables

Branch `packet/f2-6`; executor.py line count before/after; the
remaining-inline census; shared-helpers and improvement-candidates lists;
full-suite table; pinned-gate proof; `delivery/packets/F2-6-REPORT.md`.

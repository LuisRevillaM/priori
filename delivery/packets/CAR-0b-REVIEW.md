# CAR-0b Review: ACCEPTED (2026-07-18)

Reviewed at the packet tip; director's suite green; fences held. The
fragile-state predicate certifies through the reused pressure
machinery (no parallel thresholds — the charter's §3 mapping held),
GEO-0a coverage semantics govern absence, and the charter's sample-
collapse warning was honored: eligibility stays minimal. 518 eligible
episodes across seven matches with reason-coded population rows; the
zero UNKNOWN eligibility share is an observed corpus result, honestly
framed. NOTE for CAR-2a: the GALLERY-2b finding (dead-ball
transitions polluting regain populations) does NOT touch CAR-0b
(pressure eligibility is frame-evidence-based, not transition-anchor-
based) — verified by reading the predicate's inputs — but the
situation classifier must inherit the open-play law when regains
enter any class field.

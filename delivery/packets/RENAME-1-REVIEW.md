# RENAME-1 Review — Round 1: ACCEPTED

Reviewed 2026-07-05 at dbd0a23. Verified by the director's hand:

Law 1 (history never rewritten): the branch diff touches no dated
record — only RENAME-1's own report lands under delivery/packets; ADR
bodies untouched. Law 2 (attribution never disappears): the atlas
PROVENANCE.md states origin plainly; the README history paragraph
names the inspiration honestly. The classified inventory landed
BEFORE any rename, as ordered. The one guard-adjacent edit (the
attestation test's recognized identity-drift set gaining
source_file.mcp_server after the MCP prompt rename) was declared at
commit level with fail-closed reasoning — the deviation law followed
exactly. Director's full suite green.

Deliberate carries, recorded: the machine-facing registry id and
Hermes toolset identifiers keep the old string (frozen identifiers;
renaming them drifts every hash pin — scheduled for a contract-version
bump at a natural era boundary, not a text sweep). The Render
deployment hostnames are live infrastructure, renamed as an infra act
in the follow-up, not a text edit. Generated artifacts flow the
renamed source strings through sanctioned regeneration at this merge.

The project is Entrelíneas — with its history intact and its debts
named.

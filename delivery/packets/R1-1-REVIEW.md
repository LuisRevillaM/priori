# R1-1 Acceptance Review — Round 1: REJECTED

Reviewed 2026-07-04 at 9ec670c. Scaffolding conduct verified clean (registry
dispatch, bind-time rejection 6/6 probes, hash invariance across all 13
plans, eight pinned gates green, fences exact, witnesses recomputed 20/20).
Rejected on operator semantics and on the substance of the era's KPI claim.

B1 — Orientation basis wrong: goalward keyed on the invocation's
perspective team instead of the acting team's role (the kernel convention).
6/20 acceptance rows sign-flipped; two channels on one record disagree in
sign. The mirror test passed via a synthetic per-record field real records
lack.
B2 — toward_point fabricates a (0,0) axis when the declared reference field
is missing and emits PASS. Fabrication; along_lane_normal shows the correct
UNKNOWN shape. Three of four axes have zero tests.
B3 — The 7->8 was engineered: discovery space of exactly 1 by input-typing
choice; composed scalar numerically identical to the provider's existing
forward_progression_m; booked against support_depth whose ledger meaning
(supporter longitudinal distance from carrier) the composition does not
realize; the ledger flips rows by concept-name label with no
semantic-correspondence check.

## Director rulings

R-A: **The acceptance composition must realize the claimed row's own
meaning.** Round 2 builds true support_depth: the supporter-relative
vector (support-arrival / census point-pair channels) projected on the
acting-team goalward axis relative to the carrier. If those channels'
temporal types need the signature-union support the reviewer identified,
implement it now — it is scaffolding-level and R1-2's prerequisite anyway.
R-B: Orientation basis becomes a DECLARED parameter (acting_team default;
per-record resolution; basis recorded in every evidence record). The local
string vocabulary for attacking_direction is deleted.
R-C: All four axes tested; missing reference fields -> UNKNOWN everywhere;
zero-length vectors get a declared policy.
R-D: **KPI governance:** the reachability tooling gains a
target<->row semantic-correspondence requirement (R1-C hardens it; round 2
documents the correspondence explicitly in the target file). Provider-name
score bonuses are ratcheted out of "generic" search rules.
R-E: The reviewer's nine precedent notes are ADOPTED as the R1 operator
template (appended to ADR 0013); R1-2..5 packets will cite them.

## Required for round 2 (same branch)

Fix B1/B2 per R-B/R-C; rebuild the acceptance composition per R-A with the
reachability delta re-measured blind (and the discovery space size
reported honestly); update the target file with the semantic
correspondence; full-suite table; the pinned gates; report the corrected
20-row evidence with per-record orientation basis visible.

---

# R1-1 Round-2 Review: ACCEPT-WITH-FIXES — the 8 is earned in substance, certified after fix 1

Reviewed 2026-07-04 at 853cf27. B1 verified empirically (400 records
hand-recomputed against the shared orientation table, 0 mismatches,
adversarial half checked); B2 probed fixed with declared zero-length
policy; B3's view legitimate (kernel untouched, purely additive, real
supporter+reference points, first-by-arrival semantics), values
football-plausible both signs, discovery space honestly 1 and verifiable,
correspondence declared, 7->8 reproduced on a fresh ledger. Suite 385
green, attestation VERIFIED, pinned gates exact to frozen hashes.

BLOCKING FIX 1: the synthesized proof plan drops the target's declared
relation constraints (infer_parameters has no support_arrival_point_pair
branch -> catalog defaults bound, divergence unreported). Apply the
declared constraint parameters in synthesis, make unapplied constraint
keys a synthesis FAILURE or reported divergence, re-run both proof
commands, republish. Fix 2: remove tool-side provider defaults
(release/reception_ball_point). Fix 3 (minor): derive the tool's field
list from the signature; state the angle-units policy explicitly.

The 8 is certified upon fix 1's regenerated proof.

# GALLERY-2 Review: ACCEPTED (2026-07-16)

Reviewed at e1c38fe8. Fallback rung (a) fired — orientation-aware
thirds expressible from existing vocabulary. The pressing map:
2,811 observed regains → defensive 1,204 / middle 1,054 / final 500,
53 location-unknown. Table byte-reproduced by the committed
generator. Suite green (director's run, including the descriptor
rebuild exercising all 2,811 real moments). The executor's two
sandbox-blocked gates were run by the director: committed HTTP
oracle PASS against a local instance (both asks servable), and the
SEALED 2GiB proof — four attempts (docker context bloat from an
executor clone; Docker VM disk exhaustion, 18GB build cache pruned;
Render-baked dataset paths needing env-corrected mounts) — landing
at peak 515.7 MB, headroom 1,631.7 MB, sealed_2gib_proof=true.
Evidence beside this review. Ops notes ledgered: .dockerignore gains
executor-clone patterns; docker builder cache is a standing disk
hazard on this host.

Ships by explicit deploy; live oracle closes as always.

---

## Hotfix acceptance + ship — 2026-07-17

Root cause named and fixed: the descriptor code-epoch hashed the
whole service file (over-broad key — any code change orphaned every
fragment) and the one-loop build raised out entirely. Now: contract-
scoped epoch with v1→v2 fragment migration that never opens execution
payloads; per-flagship build boundaries; typed absence
(FILM_ROOM_BUNDLED_DESCRIPTOR_EVIDENCE_ABSENT) — the isolation law
implemented with the exact 115/typed-zero test. Director's legs all
green: full suite; sealed 2GiB proof on the refreshed layout (peak
504.4 MB, headroom 1,643 MB, pressing 2,811 servable); old-layout
absence proof (retention 115 + pressing typed-absent, zero payload
bytes opened). Refreshed bundle (SHA 32f44347…) uploaded to S3 by the
director; fresh presign + SHA in the Render env. ACCEPTED; shipping.

---

## Live closure — 2026-07-17

ORACLE PASS on the public URL: both asks servable — the pressing map
at 2,811 moments (defensive 1,204 / middle 1,054 / attacking 500,
53 location-unknown) beside the retention chain's 115. One final
image defect (the plan file absent — the LEGIBILITY-1 class) was
caught GENTLY by the isolation law as a typed absence, fixed by the
director in one line, and verified inside the built image before
deploy. GALLERY-2 is CLOSED live. The confident layout is public.

# LEGIBILITY-1 Review — Round 1: ACCEPT-WITH-FIXES

Reviewed 2026-07-12 at b5f6c4f. Leg zero clean (constitution, tokens,
oracles, dev sets untouched). Director's full suite green; frontend
unit modules green; the committed Playwright producer runs the N8
walkthrough verbatim and passes. The three candidate screens were
regenerated by the director's own run of the committed producer and
reviewed by the director's own eyes — the Grade-A ritual as written.

## The N8 judgment (the owner's test, applied personally)

PASS. The question renders with its clauses keyed in plain football
words ("① they win the ball back / ② carry it forward at least 3 m /
③ keep it with a completed pass"); the badges sit on the turf at
their witness moments; the PASS card narrates "① 63:12 regain →
② +11.2 m carry → ③ pass kept"; the UNKNOWN card says "couldn't see
whether ② happened — half ended" — the truncation reason in the
question's own vocabulary; and the interval card restates the
question as answered: "Of 2,811 regains, 1 completed the whole chain
①→②→③. 2,810 couldn't be fully seen — they widen the honest bounds
to [0.04%, 100%]." A first-time viewer can point at the screen and
narrate which part is which. Rule N8 is implemented.

## Executor's queued ruling: ENDORSED

Retaining certified counts (2,811 regains / 2,810 unknown) over the
packet's illustrative numbers was correct — the spec's "115" was the
chain count, not the regain denominator; deference to certified
evidence over spec illustration is exactly the right instinct and is
now precedent.

## Fixes (small, exact)

1. Microcopy (director-dictated, N7): the pre-hydration PASS-card
   text "carry witness loading" leaks a system word. Replace with
   "② watching the carry…" (filmRoomLegibility.ts:123). The ellipsis
   is the loading signal; the sentence stays in the question's world.
2. Provenance strip shows "TREE unknown" in the fixture environment.
   Verify the live surface renders the real tree hash; if the
   component defaults to "unknown" when the field is absent, make
   absence render as an em-dash with a title attribute, never the
   word "unknown" (which collides with UNKNOWN's semantic meaning on
   this surface — same word, different truth, N7 violation).
3. Re-capture the three candidates after fixes 1-2; the director
   then promotes them to committed goldens (goldens change only by
   director ruling — this review is that ruling, effective on the
   re-captured set).

## Deferred to the perceptual stack run (not this packet)

Multimodal critic panel and the genuinely-fresh cold walkthrough run
against the LIVE deployed surface after merge, per the stack order.

---

## Fix round + golden promotion — 2026-07-12

Both fixes verified in re-captured pixels by the director ("watching
the carry…" in the question's voice; tree absence as em-dash). The
three re-captured screens are PROMOTED to committed goldens at
delivery/goldens/legibility-1/ by this ruling. Merged and shipped;
live verification (real tree hash on the deployed strip) appended at
ship.

---

## Hotfix round — 2026-07-12

The live regression's diagnosis took three tries and the executor's
evidence beat both director hypotheses: not ephemeral fragments (the
rollback served 115 from the same disk), not schema drift (no typed
field changed) — the production IMAGE omitted the new
meaning-expression module, failing counterattack prewarm upstream of
the descriptor path, and the failure was swallowed into a silent
zero. Fixed at 10dbdb4, plus the durable laws regardless: fragments
rebuild from disk caches on miss-or-invalid, explicit
rebuilding/failure states replace silent zeros, and the 2GiB proof
carries real headroom this time (95.85 MiB peak, 1,952 MiB free).
Deviation (image defect) disclosed and RATIFIED. Ships on director's
suite green; live chain oracle closes as always.

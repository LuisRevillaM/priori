# LEGIBILITY-3 — composition and scope (perceptual run 2 fix packet)

Status: READY  Grade: A (dictated; report ambiguity, never improvise)
Branch: packet/legibility-3 off the frontier
Verdict + captures: delivery/packets/perceptual-run-2/. Fences
unchanged (docs/design/** director-only). THIS IS ROUND 3 of the
gate: it passes or the surface is redesigned. Every fix below is
surgical; resist any urge to restructure beyond them.

## Blocking

1. CERTIFICATION SCOPE (honesty F1): the header badge must never
   claim more than the provenance strip beneath it. Replace
   "evidence pipeline: certified" with "metric interval: certified"
   (or the exact certified scope read from data). No blanket claims.
2. INTERVAL BAR PERCEPTIBLE (F2 + craft-3): the [lower, upper] span
   renders as a clearly visible slate band (target the #8B93A0 family
   at full perceptual salience against panel; verify by pixel
   sampling in the test); observed point = thin amber tick with its
   "observed" label LEADER-LINED to the tick's actual position, never
   centered. The n=1 amber budget: "1" may wear amber; "/1 (100%)"
   renders chalk-dim (F3).
3. PARTITION STRIP ALIVE (craft A7): direct-label the segments,
   delete the legend row; nonzero segments get a minimum-visible
   width (2-3px) with the break stated on the surface ("segment
   enlarged to be visible — true share 0.04%").
4. UNKNOWN FRAME COMPOSITION (F4 + craft-1): unknown banner docks
   OUTSIDE the pitch canvas as an opaque slate strip (no translucent
   slab over pitch lines); on-pitch not-verified markers + keys wear
   SLATE ink (dashed border stays); collision-aware label placement
   (flip above/below with leader lines); pitch drawing inset so no
   entity or marker can clip at the container edge.
5. ONE QUESTION, ONE SENTENCE (craft-2): on answer, the keyed
   question panel IS the record — the ask input collapses to an empty
   "Ask another…" affordance aligned to the page grid (x=129 left
   edge family); the reconciliation sentence renders ONCE, at the
   moment list where it explains the count beneath it.

## Non-blocking (do with the same care)

6. PLAN=DOC hash: if genuinely one artifact, render once as
   "PLAN = DOC d8179…"; if distinct, fix the copy bug (verify at
   source).
7. Answer-card type budget: within three sizes (craft A3).
8. Witness-moment timing (N8): during playback, each key badge
   appears AT its witness sample and persists after (static-decal
   before its moment = the rule's violation). If replay data lacks
   per-stage witness frame ids, STOP on this item and report — never
   fake timing.
9. Scrubber/status vocabulary: "sample N of M" gains its unit once
   ("frame N of M · 25 fps"); team color legend (which color is
   which team) appears once near the pitch (coach round-2 note).

## Acceptance (the gate's own repair rides along)

The DIRECTOR repairs the capture producer (scripts/ or committed
producer path): distinct frames asserted by hash (duplicate captures
are a producer FAILURE), the frame set MUST include: landing, the ③
witness moment mid-playback, an UNKNOWN replay, warming state, empty/
error state (N2 inventory). Frontend tests: badge/segment colors
sampled per status; leader-line presence; single-render of question
and reconciliation sentence. Full-suite table. Director review with
token audit; goldens re-promoted; explicit deploy; panel round 3.

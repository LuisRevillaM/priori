# GALLERY-2 — the pressing map joins the gallery

Status: READY  Grade: B (engine composition; the one UI element is
dictated below)
Branch: packet/gallery-2 off the frontier
Oracle: generator byte-reproduces the committed table (R-Y);
descriptor fragments build under prewarm=0; local gallery oracle
green with BOTH asks servable; the ratio-first layout renders on
real data.
Headline risk: promising "attacking third" if the zone vocabulary
cannot express it — use the FALLBACK LADDER, never improvise
vocabulary.

## The ask (owner-requested, demo-critical)

"Where does each team win the ball back?" — per-team regain counts
with locations, every regain a watchable moment. The evidence is the
2,811 OBSERVED regains both flagships already stand on: real counts,
tight intervals, hundreds of moments — the ratio-first/confident
layout's first production outing.

## Fallback ladder (STOP at the first expressible rung; disclose which)

a. Regains partitioned by pitch thirds (defensive/middle/attacking,
   orientation-aware) per team per match — IF a thirds/zone partition
   is expressible from registered primitives.
b. Regains in the attacking half vs own half per team — if only a
   half-split is expressible.
c. Per-team regain counts + the full moment gallery, no spatial
   partition — always expressible (the population is certified
   machinery).
If even (c) fails to bind, STOP and report; do not touch the
registry (vocabulary expansion is F0-F1-gated, not this packet).

## Scope

1. Compose the meaning expression from EXISTING vocabulary; synthesize
   and bind through the standard envelope; execute across all seven
   matches, both perspectives.
2. Certified table + generator (byte-reproduction law) under
   delivery/packets/gallery-2-pressing-map/.
3. Register as a gallery flagship (film_room_flagship_specs) with
   descriptor fragments + hydration shards (the DEPLOY-2 lazy path;
   prewarm stays 0; 2GiB law: state headroom if memory work is
   touched).
4. UI, dictated: a two-tab ask switcher above the ANSWER panel using
   the existing header-pill style — tab labels are the two questions
   in plain words ("Where do they win it back?" / "After a regain, do
   they keep it?"); selecting a tab swaps the bootstrap answer,
   moment list, and replay context. No new colors, no new components,
   token lint applies. Extend the Playwright producer to capture the
   second tab.
5. Full-suite table; frontend unit tests for the switcher and the
   ratio-first rendering with real (non-fixture) shapes.

## Acceptance

Leg zero; director reproduces the table; token audit on new pixels;
director deploys; live oracle + a live capture of the pressing map;
goldens re-promoted including the confident layout.

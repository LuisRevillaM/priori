# Work Packet DOC-1 — Case Study Part Two, Interactive

Issued: 2026-07-02. Self-contained brief for an external executor.
Documentation packet: no runtime semantics may change.

## Ground rules

- Branch `packet/doc-1` off `codex/afl08-passport-loop` (tip `e37e69f`+).
- Read first: `docs/CASE_STUDY.md` **part two** (the narrative of record —
  the page must follow it, not paraphrase it loosely),
  `apps/workbench-alpha/src/CaseStudy.tsx` (part one's structure, styles,
  and replay components — reuse them; do not invent a new visual system),
  and the replay-packet format of `apps/workbench-alpha/public/case-study-*.json`.
- Standing bar applies: full-suite table on the committed tree in the
  report, plus `npm --prefix apps/workbench-alpha run test:unit` and
  `run build` green.
- Fences: no changes to `src/tqe/runtime/`, `semantic-registry/`,
  `generated/` (except NEW replay payloads if the established generation
  scripts put them elsewhere — follow the existing pattern for how
  case-study replay JSONs were produced; check `scripts/` for the
  generator used by part one), `frozen-expectations/`, `delivery/n1d/`.
  Replay payloads must be generated from real canonical data via the
  runtime — never hand-authored coordinates.

## Deliverable

Extend the `/case-study` route with a part-two section matching
`docs/CASE_STUDY.md` part two, in part one's narrative style ("Look for /
Proves / Does not claim" context blocks + animated replay figures), with
these four exhibits:

1. **The honest UNKNOWN.** A real J03WOY controlled-pass candidate that was
   FAIL under the old semantics and is UNKNOWN now (ideally a
   reception-window-truncated case near period end; else a sparse-release
   case). Look for: the pass and where evidence ends. Proves: the evidence
   was insufficient to decide. Does not claim: that the pass failed.
2. **The corridor that was never 0.8 seconds.** One of the fifteen M1.1
   episodes whose duration moved 0.8 → 0.6 s. Show the corridor overlay
   with open/close frames and both duration figures (old counting vs
   elapsed span).
3. **The twelfth hero moment.** The possession admitted by the unified lane
   geometry: corridor + destination region under the five-lane partition,
   with a caption noting the old fractional partition excluded it.
4. **One pitch, one partition.** A static or lightly animated figure of the
   five-lane partition with the y = 8.0 m marker, captioned with the
   part-one-style contrast: what the two old models each claimed, what the
   one declared model now claims.

Also: the hero genealogy (14 → 11 → 12) rendered as a simple three-step
figure with one-line causes — no chart library, match existing styling.

Keep part one byte-untouched except whatever minimal navigation/anchor is
needed to reach part two.

## Report

`delivery/packets/DOC-1-REPORT.md`: exhibit provenance (which real
moments/frames, generated by which command), the full-suite table,
workbench unit-test and build results, and screenshots or a textual
walkthrough of the rendered section.

# DEPLOY-1C Review: ACCEPT-WITH-RULING (merge queued behind HERMES-2)

Reviewed 2026-07-10 at 4034d57. Leg zero clean; fenced oracle PASS
locally (ready-state, interval law, servable replay window); full
suite 590 green with the worktree corpus-wiring ripple disclosed;
review depth: focused director pass on the serving-surface diff (the
fenced oracle carries the load-bearing behavior; no UI pixels
changed), recorded per the calibration rule.

## Ruling on DEPLOY-1C-D1: RATIFIED as a time-bounded exception

The table-served gallery shows what the committed certified evidence
honestly carries (interval, certified rows, 28 table-backed partition
previews) — labeled as certified, never impersonating the 115
chain-record moments that R-BB mandates for the EXECUTION path. The
exception expires when DEPLOY-2 refreshes the bundle caches and
execution prewarm returns to small instances; until then the ledger
carries it. Honest-and-degraded beats warming-forever; silent
degradation would have been a REJECT.

The live-URL oracle run plus an explicit replay-window request, post
merge and deploy, closes this packet — merge queued behind HERMES-2
per the one-writer law (Sol-1 holds the main tree).

---

## Live closure — 2026-07-10

The strict oracle PASSES against the deployed URL (state=ready,
interval PASS, moment PASS) and an explicit replay-window request
served 101 canonical frames (J03WOH firstHalf) over the public
internet. DEPLOY-1C is CLOSED; DEPLOY-1B's live-PASS condition is
satisfied by the same deployment. The public gallery is demoable.

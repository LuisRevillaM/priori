# THE ENGINE RUN — standing autonomous mandate (owner-granted 2026-07-18)

Owner's words: "devise an autonomous run where you execute what's
authored or proceed with authoring at the frontier to continue
execution until... we need to ship the engine. Finish our compiler,
algebra, all of it... I can trust you to run with this and finish,
really finish, prioritizing the engine."

## Ship criteria (the run ends when ALL are true)

E1. ALG era certified: K3 tri-state logic, population set algebra,
    generalized sequence_pattern (N/optional/negative stages),
    temporal relations, numeric field domains (sum/mean/share) +
    general comparison — registered, certified, in the pack.
E2. GEO-2..6 certified (region membership, point-pair, team
    distribution, ball control, pass attempts) + their charter
    recipes.
E3. Move 1 standing: the correspondence oracle — contrast sets,
    paraphrase families (absorbs CAP-1), IR→plan equivalence, frozen
    + rotating panels — running as a fenced instrument.
E4. Move 2 landed: CoachQueryIR — one canonical typed IR compiling
    deterministically into the FINISHED algebra; bounded search
    demoted to offline discovery; the three-proof decomposition
    (parse fidelity / program correctness / empirical validity)
    reported per answer.
E5. Coach vocabulary layer: saved, versioned, parameterized
    definitions over the algebra.
E6. Hermes serves the full grammar: pack projections regenerated,
    dev evals re-run, capability honestly reported.
E7. In-flight product truth completes: GALLERY-2b, FILM-3.
E8. Span Dossier Contract standing (ADR 0021) — the uniform span
    artifact every match-producing query emits.
E9. The Tier Law ratified constitution-grade (ADR 0021) and enforced
    on every serving surface.

## Standing mechanics (unchanged laws)

Sol executor line single-file; counsel exchanges on fresh instances
(sealed director position first, convergence, era ADRs ratified —
owner ratification ONLY for vision-bearing definitions, else
director rules); one packet one risk; STOP laws; census ratchets by
name; face-validity leg on watchable tables; capacity walls →
sealed-design backoff and resume; STATE current at every merge; the
director's own suite before every merge; deploys explicit.

## Priority within the run

Truth defects > ALG spine (ALG-1→2→5→3→4) > Move 1 (interleaved) >
GEO-2..6 (interleaved) > Move 2 (only after ALG stable + Move 1
oracle exists) > E5/E6 closers. CAR stays PARKED until the run ends.

## What still waits on the owner (never blocking the run)

Taste calibration + delight pass; sealed exam SCP2-E; any spend
beyond standing authorizations; Hermes re-auth if the credential
dies. Everything else proceeds without asking.
